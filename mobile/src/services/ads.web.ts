// Web stub — react-native-google-mobile-ads has no web build. Same exported
// signatures as ads.native.ts so callers don't need a platform check.
export async function initAds(): Promise<void> {}

export async function maybeShowInterstitial(): Promise<void> {}
