# Expo exposes vars prefixed EXPO_PUBLIC_ to client code. See
# docs/ENV_VARS.md at the repo root for where each value comes from — none
# of these accounts exist yet, so this file is placeholders until they do.

EXPO_PUBLIC_API_BASE_URL=http://localhost:8001
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY=
